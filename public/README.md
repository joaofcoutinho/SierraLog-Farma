# Public assets

Coloque aqui as imagens referenciadas pela LP:

- `hero-farma.jpg` — imagem do hero (recomendado 1200x1500, foco em centro de armazenagem/operação farma)
- `og-farma.jpg` — imagem de Open Graph (1200x630)
- `favicon.ico` — favicon padrão

Enquanto os arquivos finais não estiverem prontos, o `next/image` apenas mostra o placeholder de fundo neutro do container.
